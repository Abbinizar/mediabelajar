<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abbinizar.mediabelajar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_open_book"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_open_book"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".detailKuis"></activity>
        <activity android:name=".detailMateri" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".KuisActivity" />
        <activity android:name=".EssayActivity" />
        <activity android:name=".materiActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>